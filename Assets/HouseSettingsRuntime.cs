using UnityEngine;

// Kept as an empty placeholder because the current project file still references this path.
// House settings hierarchy creation now lives in Assets/Editor/HouseSettingsHierarchyBuilder.cs.
public sealed class HouseSettingsRuntime : MonoBehaviour
{
}
